# Vista previa — Adrián Roma

Copia compilada del portfolio, publicada **solo para que el cliente pueda verlo**
mientras se decide el dominio definitivo. No es la web final y no se indexa en
buscadores (`robots.txt` + `noindex`).

El código fuente vive en otro repositorio, privado. Aquí solo está el resultado
del build, así que no se edita a mano: se regenera con `npm run build` y se
vuelve a subir.
